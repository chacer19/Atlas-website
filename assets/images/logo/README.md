# Logo

No logo image file exists yet. The "Atlas" wordmark in the header/footer (`.brand-mark`, `.brand-text`) is currently pure CSS/text — a styled "A" monogram plus type, not an image.

If the firm wants to replace this with a real logo image, drop it here (e.g. `atlas-logo.svg` or `.png`) and swap the `.brand` markup in each page's header/footer to reference it with an `<img>` tag. That's a small design/markup change, not covered by this data-file refactor since there's no existing image to preserve.
